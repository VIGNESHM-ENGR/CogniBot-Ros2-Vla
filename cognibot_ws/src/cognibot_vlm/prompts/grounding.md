Locate every object matching "{label}" in the image.
Answer with JSON only, no prose: a list of objects with "bbox_2d" as [x1, y1, x2, y2] in 0–1000 normalized image coordinates and "label" as the object name.
If nothing matches, answer [].
